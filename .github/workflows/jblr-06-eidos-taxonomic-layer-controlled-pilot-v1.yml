name: JBLR 06 EIDOS TAXONOMIC LAYER CONTROLLED PILOT V1

on:
  workflow_dispatch:
  push:
    branches:
      - 06-taxonomic-genus-reconciliation-v3
    paths:
      - 'app/data/taxonomy/eidos-controlled-pilot-trigger-v1.txt'

permissions:
  contents: write

concurrency:
  group: jblr-06-eidos-taxonomic-layer-controlled-pilot-v1
  cancel-in-progress: false

jobs:
  eidos-controlled-pilot:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      EIDOS_PILOT_OUT: artifacts/eidos_taxonomic_layer_pilot_v1
    steps:
      - name: Checkout actor 06 branch
        uses: actions/checkout@v4
        with:
          ref: 06-taxonomic-genus-reconciliation-v3

      - name: Install dependency
        run: python3 -m pip install requests

      - name: Execute controlled EIDOS pilot
        id: pilot
        continue-on-error: true
        run: python3 app/scripts/eidos-taxonomic-layer-controlled-pilot-v1.py

      - name: Materialize pilot evidence
        if: always()
        shell: bash
        run: |
          TARGET='evidence/06_stimes/eidos_taxonomic_layer_controlled_pilot_v1/latest'
          rm -rf "$TARGET"
          mkdir -p "$TARGET"
          if [ -f "$EIDOS_PILOT_OUT/EIDOS_TAXONOMIC_LAYER_CONTROLLED_PILOT_V1.json" ]; then
            cp "$EIDOS_PILOT_OUT/EIDOS_TAXONOMIC_LAYER_CONTROLLED_PILOT_V1.json" "$TARGET/"
          else
            printf '%s\n' '{"pilot_id":"EIDOS_TAXONOMIC_LAYER_CONTROLLED_PILOT_v1","overall":"EXECUTION_ARTIFACT_MISSING","global_rematerialization_allowed":false}' > "$TARGET/EIDOS_TAXONOMIC_LAYER_CONTROLLED_PILOT_V1.json"
          fi
          printf '%s\n' \
            "github_run_id=${GITHUB_RUN_ID}" \
            "source_commit=${GITHUB_SHA}" \
            'authority=MITECO_IEPNB_EIDOS' \
            'endpoint=https://datos.iepnb.es/sparql' \
            'canonical_field_directive=JBLR-EVT-0000-20260820-TAXONOMIC-FIELDS-001' \
            'canonical_execution_directive=JBLR-EVT-0000-20260820-TAXONOMIC-EXEC-001' \
            > "$TARGET/RUN_METADATA.txt"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add "$TARGET"
          if git diff --cached --quiet; then
            echo 'No pilot evidence change to commit'
          else
            git commit -m 'Materialize controlled EIDOS taxonomic pilot v1 [skip ci]'
            git push origin HEAD:06-taxonomic-genus-reconciliation-v3
          fi

      - name: Enforce pilot PASS gate
        if: steps.pilot.outcome != 'success'
        run: |
          echo 'Controlled EIDOS pilot did not PASS. Global rematerialization remains blocked.'
          exit 5

      - name: Upload pilot artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: EIDOS_TAXONOMIC_LAYER_CONTROLLED_PILOT_V1
          path: artifacts/eidos_taxonomic_layer_pilot_v1/
          if-no-files-found: warn
          retention-days: 30
